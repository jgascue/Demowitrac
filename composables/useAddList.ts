/* import { reactive, toRefs } from 'vue'

export setup {
    const data = reactive ({
        task: null,
        lista: [],
    }),

    const addTask = () => {
        if (data.task.length > 1 ) {
            data.list.push(data.tarea)
        }
        data.task = null
    }

    const deleteTask = (index) = {
        data.list.splice (index, 1)
    }

return { ...toRefs(data), addTask, deleteTask }
} */